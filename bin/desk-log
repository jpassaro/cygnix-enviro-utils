#!/usr/bin/env bash
# Log a session telemetry entry with timestamp.
# Usage: desk-log "started session for okr-fanout-rollout, timebox 45m"

set -euo pipefail

mkdir -p "$HOME/desk/log"
printf '%s %s [%s]\n' "$(date +%H%M%S)" "$*" "$JP_TTY${JP_TTY:+:}$PWD" >> "$HOME/desk/log/$(date +%Y%m%d)"
